# A collection of functions for strings.
def reverse(s: str) -> str:
    """Returns the reversed version of a string."""
    return s[::-1]


def uppercase(s: str) -> str:
    """Returns the uppercase version of a string."""
    return s.upper()
